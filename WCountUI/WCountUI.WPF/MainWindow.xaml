<Window x:Class="WCountUI.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WCountUI.WPF" xmlns:Localizations="clr-namespace:WCountUI.WPF.Localizations"
        mc:Ignorable="d"
        Title="{Binding Resources.App_Labels_Name}" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2">
            <Label Content="{Binding Path=WordCount}" FontStyle="Normal" FontWeight="SemiBold"/>
            <Label Content="{Binding Path=WordCountText}" Margin="20 0 0 20"/>

            <Label Content="{Binding Path=CharacterCount}" FontStyle="Normal" FontWeight="SemiBold"/>
            <Label Content="{Binding Path=WordCountText}" Margin="20 0 0 20"/>
        </StackPanel>
        
        <TextBox x:Name="textBox" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="2" Text="{Binding Path=Text}"
                 MinLines="10" MinWidth="600" MinHeight="350" AcceptsReturn="True" TextChanged="textBox_TextChanged"/>
    </Grid>
</Window>
